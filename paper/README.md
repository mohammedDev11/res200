# Main Paper

- `drafts/` contains editable, versioned working drafts.
- `reviews/` contains feedback, annotations, and review copies.
- `final/` contains approved or submitted versions.

Use sequential versions such as `res200_paper_v001.docx`. Increment the version
for meaningful revisions and record major changes in `../context/PROGRESS.md`.

